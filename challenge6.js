
let array = [
    15, 14,  9, 11,  4,
    12, 12, 11,  3, 12,
    14,  4
  ]
const new_array = []

new_array.push(array[0])

array.forEach((index) => {
    if (array[0]>array[index]) {
        
    }
    




    // if (new_array[0] >= array[index]) {
    //     new_array.unshift(array[index])
    // }else if (new_array[0]<= array[index]) {
    //     new_array.push (array[index])
    // }else { 
    // }
});

console.log (new_array)